name: exercise_tracker_prototype
description: Offline-first GPS tracking prototype for exercise debriefing.
publish_to: "none"
version: 0.2.0+2
environment:
  sdk: ">=3.5.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.0
  geolocator_android: ^5.0.3
  geolocator_apple: ^2.3.14
  http: ^1.3.0
  sqflite: ^2.4.0
  path: ^1.9.0
  uuid: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
